<%- include('../partials/header') %>

<div class="wishlist-main">
    <div class="container">
        <h3 class="wish-head">Order History <i class="ri-shopping-bag-2-line"></i></h3>
        <div class="row">

            <% orders.forEach((item)=>{ %>

                <div class="col-12">
                    <div class="wish-item">
                        <a href="/order/<%=item._id%>" class="order-img text-dark">
                            <img src="/product-images/<%=item.product.mainImage.filename%>" alt="">
                        </a>
                            <a href="/order/<%=item._id%>" class="order-detail text-dark">
                                <b><%= item.product.name %> </b>
                                <h5 class="mt-2">₹<%= item.total  %> </h5>
                            </a>
                            <div class="order-cancel">
                                <b class="">Quntity : <%= item.quantity  %> </b>

                            </div>
                            <div class="order-status d-flex">
                                <b>Status :</b> 
                                <div class="<%=item.orderStatus%>"> <%=item.orderStatus%></div>
                                <!-- <button>Cancel</button> -->
                            </div>
                        </div>
                </div>
            <% }) %> 
           
        </div>
    </div>
</div>